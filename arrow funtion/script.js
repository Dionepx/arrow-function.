function soma(x, y) {
     return x + y
}

const soma1 = soma(2, 3)

const multiplicacao = function (x, y) {
     return x * y
}

const mult_1 = multiplicacao(3, 2)
console.log(mult_1)

const dividir = (x, y) => {
     return x / y
}

const objeto = () => ({nome: "Dione", sobrenome: "Silva"});